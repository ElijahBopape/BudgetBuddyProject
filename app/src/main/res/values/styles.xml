<?xml version="1.0" encoding="utf-8"?>
<!-- Here we keep our commonly used styles to avoid typing them again and again -->
<resources>
    <!-- A nice card with subtle shadow and rounded corners -->
    <style name="BlueCard" parent="Widget.Material3.CardView.Filled">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">@color/surface</item>
    </style>

    <!-- Those cute little circles that wrap the icons -->
    <style name="CircleIconBg">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:background">@drawable/circle_icon_bg</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/primary</item>
        <item name="android:gravity">center</item>
    </style>
</resources>